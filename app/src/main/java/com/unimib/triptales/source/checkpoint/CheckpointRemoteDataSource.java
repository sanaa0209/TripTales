package com.unimib.triptales.source.checkpoint;

import com.google.firebase.database.DatabaseReference;
import com.google.firebase.database.FirebaseDatabase;

public class CheckpointRemoteDataSource implements BaseCheckpointRemoteDataSource {

    /*
        private final DatabaseReference databaseReference;
    public CheckpointRemoteDataSource(String userId, String diaryId) {
        this.databaseReference = FirebaseDatabase.getInstance()
                .getReference("users")
                .child(userId)
                .child("diaries")
                .child(diaryId);
    }

     */
}
