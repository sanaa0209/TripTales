<?xml version="1.0" encoding="utf-8"?>

<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@android:color/darker_gray"> <!-- Colore del ripple -->
    <!-- Il background sotto l'effetto ripple -->
    <item>
        <shape>
            <!-- Colore di sfondo del bottone -->
            <solid android:color="@color/white_dark" />
            <!-- Angoli arrotondati -->
            <corners android:radius="20dp" />
        </shape>
    </item>
</ripple>